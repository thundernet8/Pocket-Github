export enum IssueFilter {
    ASSIGNED = "assigned",
    CREATED = "created",
    MENTIONED = "mentioned",
    SUBSCRIBED = "subscribed",
    ALL = "all"
}

export enum IssueState {
    OPEN = "open",
    CLOSED = "closed",
    ALL = "all"
}

export enum IssueSort {
    CREATED = "created",
    UPDATED = "updated",
    COMMENTS = "comments"
}
